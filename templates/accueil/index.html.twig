{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <div class="d-flex flex-column justify-content-center align-items-center pt-5 text-center ">
        <h1 class="display-3 fw-bold mb-3 ">Bienvenue à <span style=" background: green; padding: 5px; border-radius: 15px; transform: skew(-20deg); border-bottom: white 2px solid"> <span>Zoo</span><span style="color: yellow">t<span>o</span>pia</span></span></h1>
        <p class="lead mb-5" style="max-width: 600px; color: white">
            Gérez vos enclos et vos animaux facilement avec notre interface intuitive.
        </p>

        <div class="d-flex gap-3 flex-wrap justify-content-center">
            <a href="{{ path('app_enclos_index') }}" class="btn btn-primary btn-lg shadow-sm d-flex align-items-center gap-2">
                 Enclos
            </a>
            <a href="{{ path('app_animaux_index') }}" class="btn btn-info btn-lg shadow-sm text-white d-flex align-items-center gap-2">
                 Animaux
            </a>
            <a href="{{ path('app_enclos_new') }}" class="btn btn-success btn-lg shadow-sm d-flex align-items-center gap-2">
                 Ajouter un enclos
            </a>
            <a href="{{ path('app_animaux_new') }}" class="btn btn-success btn-lg shadow-sm d-flex align-items-center gap-2">
                 Ajouter un animal
            </a>
        </div>
    </div>

    <style>
        .btn-lg {
            transition: transform 0.2s, box-shadow 0.2s;
        }
        .btn-lg:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0,0,0,0.15);
        }
    </style>
{% endblock %}
