{% extends 'base.html.twig' %}

{% block title %}Nouvel Animal{% endblock %}

{% block body %}
    <h1>Ajoutez un nouvel animal</h1>

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger">
            {{ message }}
        </div>
    {% endfor %}

    {{ include('animaux/_form.html.twig') }}
<br>
    <a class="btn btn-warning" href="{{ path('app_animaux_index') }}">Retour à la liste</a>
{% endblock %}
